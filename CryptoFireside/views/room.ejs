<!DOCTYPE html>
<html>

<head>
  <% include global/header%>
</head>
<main class="main-content">
  <script>
    const roomName = "<%= feedback.roomName %>"
  </script>

  <body>
    <% include global/sidebar%>
    <META HTTP-EQUIV="CACHE-CONTROL" CONTENT="NO-CACHE">
    <div class="container">
      <div class="row">
        <% for (var i =0; i < feedback.videos.length;  i++ ) { %>
        <div class="col">
          <div class="card" style="width: 30rem; height: 15rem;">
            <iframe id="player" width="100%%" height="100%" src="<%=feedback.videos[i].url%>">
            </iframe>
          </div>
        </div>
        <%}%>
        <% for (var i =0; i < 3-feedback.length%3;  i++ ) { %>
        <div class="col">

        </div>
      </div>
      <%}%>
      </div>

      </div>
      <div class="col">
        <div id="message-container"></div>
        <form id="send-container">
          <input type="text" id="message-input">
          <button type="submit" id="send-button">Send</button>
        </form>
      
          </div>
      </div>
    </div>
  </div>
  </div>
  </div>

  
  </body>
  <% include global/footer%>
</main>

</html>